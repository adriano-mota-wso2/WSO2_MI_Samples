openapi: 3.0.1
info:
  title: SendMessageAPI
  description: API Definition of SendMessageAPI
  version: 1.0.0
servers:
- url: http://localhost:8290/messageSender
paths:
  /send:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
